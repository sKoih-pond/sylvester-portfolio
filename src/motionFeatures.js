// Lazy-loaded Framer Motion feature bundle (DOM animations + layout projection).
// Imported dynamically by LazyMotion so these features are code-split into their
// own chunk and load after first paint, instead of bloating the initial JS.
export { domMax as default } from "framer-motion";
